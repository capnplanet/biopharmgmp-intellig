# BioPharm GMP Intelligence Platform - Environment Configuration
# Copy this file to .env and configure for your environment

# ============================================================================
# Frontend Configuration
# ============================================================================

# On-Premise LLM Gateway (Optional)
# Configure if using an on-premise LLM instead of the default mock
# VITE_ONPREM_LLM_ENDPOINT=https://llm.yourcompany.com/v1/chat
# VITE_ONPREM_LLM_TOKEN=your-llm-api-token

# Backend URL (for Docker/development setups)
# VITE_BACKEND_URL=http://localhost:5000

# ============================================================================
# Backend API Configuration
# ============================================================================

# Server Port
PORT=5000

# Node Environment
NODE_ENV=production

# Authentication Token (REQUIRED for production)
# Generate a secure token with: openssl rand -base64 32
AUTH_TOKEN=your-secure-auth-token-here

# Role-Based Access Control (Optional)
# Enable RBAC to restrict API endpoints based on user roles
RBAC_ENABLED=true

# Archive Settings
# Enable immutable archive for audit compliance
ARCHIVE_ENABLED=true
ARCHIVE_DIR=/app/data/archive

# ============================================================================
# Cloud Deployment Configuration
# ============================================================================

# AWS Configuration (if deploying to AWS)
# AWS_REGION=us-east-1
# AWS_ACCOUNT_ID=123456789012

# Azure Configuration (if deploying to Azure)
# AZURE_SUBSCRIPTION_ID=your-subscription-id
# AZURE_RESOURCE_GROUP=biopharmgmp-production-rg
# AZURE_LOCATION=eastus

# ============================================================================
# Optional: Advanced Configuration
# ============================================================================

# Backend Auth for Frontend (Optional)
# Used when frontend needs to authenticate with backend
# VITE_BACKEND_AUTH_TOKEN=your-backend-auth-token

# RBAC User Role (Optional)
# Default role for API requests when RBAC is enabled
# Options: Admin, Quality Approver, Supervisor, Operator, System
# VITE_RBAC_ROLE=Admin

# Logging Level (Optional)
# LOG_LEVEL=info

# ============================================================================
# Docker Compose Specific
# ============================================================================

# Database connection (if using external database)
# DATABASE_URL=postgresql://user:pass@localhost:5432/biopharmgmp

# Redis connection (if using Redis for caching)
# REDIS_URL=redis://localhost:6379
